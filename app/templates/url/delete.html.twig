{% include 'default/_form.html.twig' with {
    page_title: 'title.url_delete'|trans({'%id%': url.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'url_index',
} %}
