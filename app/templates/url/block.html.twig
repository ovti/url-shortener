{% include 'default/_form.html.twig' with {
    page_title: 'title.url_block'|trans({'%id%': url.id|default('')}),
    form: form,
    submit_label: 'action.block'|trans,
    back_to_list_path: 'url_list',
} %}