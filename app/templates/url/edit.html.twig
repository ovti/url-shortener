{% include 'default/_form.html.twig' with {
    page_title: 'title.url_edit'|trans({'%id%': url.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'url_index',
} %}