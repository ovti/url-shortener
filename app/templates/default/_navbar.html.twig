<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand mx-2" href="{{ path('url_create') }}">URL Shortener</a>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('url_index') }}">{{ 'title.url_list'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('tag_index') }}">{{ 'title.tag_list'|trans }}</a>
                </li>
        </ul>
    </div>
</nav>
